/** @type {import('next').NextConfig} */
// const nextConfig = {};

const nextConfig = {
    images: {
      domains: [
        'mattfarley.ca', 
        'drive.google.com', 
        'avatars.githubusercontent.com'],
    },
  };
  

export default nextConfig;


