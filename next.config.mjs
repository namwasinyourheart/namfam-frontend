/** @type {import('next').NextConfig} */
// const nextConfig = {};

const nextConfig = {
    images: {
      domains: ['mattfarley.ca', 'drive.google.com'],
    },
  };
  

export default nextConfig;


